'use strict';

/**
 * project-klm-architekten service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::project-klm-architekten.project-klm-architekten');

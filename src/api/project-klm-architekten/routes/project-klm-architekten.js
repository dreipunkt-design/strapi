'use strict';

/**
 * project-klm-architekten router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::project-klm-architekten.project-klm-architekten');
